import { ReactNode } from 'react'
import { Box } from '@mui/material'
import * as S from './styles'

type T{{pascalCase name}}Props = {
  children: ReactNode
}

export const {{pascalCase name}} = ({ children }: T{{pascalCase name}}Props) => {
  return (
    <Box sx={S.Wrapper}>{children}</Box>
  )
}
